// Навык «рецепты»: чистая функция над data/recipes.md — заранее отобранным списком того,
// что человек готовит и правда ест. Обёртки tool() здесь больше нет: рецепты модели
// показывает MCP-сервер (тул list_recipes).
//
// Смысл навыка — чтобы коуч не выдумывал блюда, которые формально проходят по
// ограничениям, но человеком не будут приготовлены никогда.

import { readData } from './dataFiles';

export function listFavoriteRecipes(): string {
  return readData('recipes.md');
}
