// Страница — только оболочка: всё, что происходит, происходит в чате. Шапка с меткой
// и выходом в новый чат живёт внутри Chat, потому что кнопка там клиентская, а двух
// планок над одной лентой быть не должно.

import { Chat } from './chat';

export default function Page() {
  return (
    <div className="shell">
      <main>
        <Chat />
      </main>
    </div>
  );
}
