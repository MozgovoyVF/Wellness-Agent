// Загрузка .env для скриптов. Внутри Next этого не нужно — он читает .env сам, до кода
// приложения; вне Next читать некому.
//
// Модуль, а не строка в скрипте: ESM выполняет тела импортированных модулей раньше тела
// импортирующего, а модель агенты и трейс берут из process.env. Вызов в теле скрипта
// опоздал бы. Поэтому импорт этого файла стоит первым — до импорта харнесса.

try {
  process.loadEnvFile();
} catch {
  // .env может не быть: ключ разрешено передать и через окружение. Если его нет вовсе,
  // об этом скажет сам харнесс, когда полезет настраивать клиент.
}
